[template]
src   = "api.tmpl"
dest  = "/etc/nginx/sites-available/api"
owner = "root"
group = "root"
mode  = "0755"
keys = ["/apollo/api", "/apollo/environment"]
reload_cmd = "supervisorctl restart nginx"
